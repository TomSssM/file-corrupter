export enum CORRUPT_SIZE {
    BYTE = 'byte',
    BIT = 'bit',
}

export enum CORRUPT_TYPE {
    DELETE = 'delete',
    SWAP = 'swap',
}

export enum CORRUPT_POSITION {
    START = 'start',
    MIDDLE = 'middle',
    END = 'end',
    RANDOM = 'random',
}
